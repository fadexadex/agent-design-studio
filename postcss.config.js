export default {
    plugins: {
        '@tailwindcss/postcss': {},
        autoprefixer: {}, // autoprefixer is actually included in tailwindcss v4 usually, but keeping it doesn't hurt or might be needed.
    },
}
